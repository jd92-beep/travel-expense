# Admin Console 獨立部署 Plan

## 📋 問題
Admin Console 錯誤地加入咗 compact app。需要分離成獨立 app。

## ✅ Plan

### Phase 1: 從 Compact App 移除 Admin Console
- 刪除 `app-compact/src/tabs/Admin.tsx`
- 刪除 `app-compact/src/lib/adminApi.ts`
- 刪除 `app-compact/src/lib/adminTypes.ts`
- 從 `app-compact/src/lib/tabs.ts` 移除 admin tab
- 從 `app-compact/src/App.tsx` 移除 Admin routing
- 從 `app-compact/src/components/Shell.tsx` 移除 admin icon/copy/filter

### Phase 2: 更新獨立 app-admin-kanban
- 更新 `src/lib/types.ts` 加入新 fields（modelName, lastSyncAt, notionStatus 等）
- 更新 `src/lib/adminApi.ts` 加入 testProvider function
- 更新 `src/App.tsx` 加入 LLM models 列表、test provider 按鈕、itinerary viewer、errors24h
- 更新 `src/styles.css` 加入新樣式

### Phase 3: Deploy
- Build + deploy to Vercel
- Smoke test

## 🔍 Verification
1. Compact app 冇 Admin tab
2. 獨立 app 可以登入 + 見到 live data
3. 所有功能 work
