// Local dev secrets — desktop testing only.
//
// Copy this file to `secrets.local.js` (gitignored) and fill in real keys.
// index.html reads `window.DEV_SECRETS` at load time and uses it only when
// nothing else is configured.
//
// On the deployed GitHub Pages site, MiniMax/ZAI keys may come from GitHub
// repository secrets (Settings → Secrets and variables → Actions), NOT from
// this file. Kimi is deliberately excluded from GitHub Pages injection because
// the generated HTML is public; use this local file or Settings on the device.
// The deploy.yml workflow only replaces __MINIMAX_KEY__ / __ZAI_KEY__.
//
// Where to get real values:
//   MiniMax  → https://platform.minimaxi.com  (sk-cp-... prefix)
//   ZAI/GLM  → https://open.bigmodel.cn       (32hex.base62 format)
//   Kimi     → https://platform.kimi.ai        (sk-kimi-... prefix)
//
// IMPORTANT: if you commit this file by mistake, rotate all tokens immediately.
window.DEV_SECRETS = {
  minimaxKey: 'REPLACE_WITH_MINIMAX_PORTAL_ACCESS_TOKEN',
  zaiKey: 'REPLACE_WITH_ZAI_API_KEY',
  kimiKey: 'REPLACE_WITH_KIMI_KEY',
};
